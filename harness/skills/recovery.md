# Bounded recovery

After a recoverable tool error, inspect the error class, correct only the failed input, retry once,
and verify the resulting state. Stop after the retry limit or when authorization is uncertain.
